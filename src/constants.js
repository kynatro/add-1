/** @constant Number The amount to add to each number */
const ADD1_ADD = 1;

/** @constant Number The amount of beats to allow input */
const ADD1_BEAT_INPUT = 2;

/** @constant Number The amount of beats to wait before allowing input */
const ADD1_BEAT_WAIT = 2;

/** @constant Number The amount of ms to wait between each beat */
const ADD1_BEAT_INTERVAL = 1000;

/** @constant Number The amount of digits in the number to increment */
const ADD1_DIGITS_AMOUNT = 4;

export {
  ADD1_ADD,
  ADD1_BEAT_INPUT,
  ADD1_BEAT_WAIT,
  ADD1_BEAT_INTERVAL,
  ADD1_DIGITS_AMOUNT
}